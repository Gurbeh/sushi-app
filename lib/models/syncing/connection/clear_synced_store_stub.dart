import 'package:drift/drift.dart';

Future<void> clearSyncedStoreRows(GeneratedDatabase db, String sqliteBaseName) async {
  throw UnsupportedError(
    'clearSyncedStoreRows: unsupported platform (no dart:html / dart:io).',
  );
}
